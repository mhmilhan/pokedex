name: Hello world!

on:
  push:

jobs:
  hello-world-job:
    runs-on: ubuntu-20.04
    steps:
      - name: Say hello
        run: echo "hello world"
      - name: Print date
        run: date
      - name: List files
        run: ls -l
