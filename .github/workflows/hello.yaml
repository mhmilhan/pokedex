name: Hello world!

on:
  push:

jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello
        run: echo "hello world"
      - name: Print date
        run: date
      - name: List files
        run: ls -l
